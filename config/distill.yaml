lora:
  rank: 128
  alpha: 128
  dropout: 0.0

training:
  epochs: 4
  learning_rate: 5e-4
  per_device_batch_size: 2
  batch_size: 32
  weight_decay: 0.1
  max_grad_norm: 1.0
  lr_scheduler: "cosine"
  warmup_ratio: 0.1